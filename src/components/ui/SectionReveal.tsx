/**
 * Placeholder — sticky sections overlap naturally via CSS.
 * No GSAP dimming/shift applied here because CSS filter
 * creates a new containing block that breaks GSAP's internal
 * position:fixed pins (journey ruler, image stack, etc).
 */
export default function SectionReveal() {
  return null
}
